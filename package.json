{
  "dependencies": {
    "react-router-dom": "^6.23.0"
  },
  "name": "vis_project",
  "description": "Repositář projektu do předmětu Vývoj informačních systémů",
  "version": "1.0.0",
  "main": "index.js",
  "devDependencies": {},
  "scripts": {
    "build": "cd backend && npm install && npm run build  && cd ../frontend && npm install && npm run build",
    "start": "TG_ALLOW_MIXED=ALLOW  node backend/build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/covman03/VIS_project.git"
  },
  "author": "",
  "license": "ISC"
}
